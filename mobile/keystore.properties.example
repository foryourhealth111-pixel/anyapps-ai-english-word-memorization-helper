# Copy to `mobile/keystore.properties` for local release signing.
# Do not commit the real file.

# Relative to `mobile/` directory, or absolute path.
WORDCOACH_RELEASE_STORE_FILE=keystore/wordcoach-release.jks
WORDCOACH_RELEASE_STORE_PASSWORD=replace-with-strong-store-password
WORDCOACH_RELEASE_KEY_ALIAS=wordcoach
WORDCOACH_RELEASE_KEY_PASSWORD=replace-with-strong-key-password
